// Organisation name shown in report headers. There is no org table (single-org,
// single-instance per ADD); configurable via env, with a sensible default.
export const ORG_NAME = process.env.ORG_NAME ?? "Our Organization";
